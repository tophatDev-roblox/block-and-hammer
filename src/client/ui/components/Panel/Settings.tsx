import React from '@rbxts/react';

import BasePanel from './BasePanel';

const Settings: React.FC = () => {
	return (
		<BasePanel
			route={'/game/side-menu/settings'}
			title={'Settings'}
		>
			
		</BasePanel>
	);
};

export default Settings;
